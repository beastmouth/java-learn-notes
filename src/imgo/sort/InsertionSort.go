package sort

import "fmt"

func InsertionSort(numbers []int)  {
	//length := len(numbers)
	//var temp int
	//for i := 1; i < length; i++ {
	//	for j := 0; j < i; j++ {
	//		if numbers[i] < numbers[j] {
	//
	//		}
	//	}
	//}
	//return numbers
	fmt.Printf("hello")
}
